# 一线通信异步轮询协议重构总结

## 1. 本轮目标

本轮重构目标不是简单合并文件，而是把原来“发一帧、等一帧回复、再发下一帧”的同步写法，改成更好理解、更适合现场调试的异步轮询写法。

核心原则：

- `ch.c` 只负责充电流程控制，不解析协议细节。
- `uart_1_wire.c` 只负责一线通信收发、解析、超时判断。
- 收发异步，不再做“等待某帧回复”的复杂状态机。
- 变量不要泛滥，小函数不要泛滥，宏定义不要泛滥。
- 代码优先让同事能顺着看懂，不追求炫技或过度抽象。

---

## 2. 通信总体规则

### 2.1 接收规则

接收不再假设缓存第 0 字节就是帧头，而是采用扫描式解析：

1. 串口 FIFO 有数据时，先做“模拟接收空闲”判断。
2. 如果本次 FIFO 长度和上次不同，说明可能还在接收，先不解析。
3. 如果连续两次 FIFO 长度相同，认为这一批数据接收完成。
4. 将 FIFO 数据搬到协议接收缓存。
5. 从缓存头到尾 `for/while` 扫描可能的 CMD。
6. CMD 已知后，根据 CMD 查固定应答长度。
7. 长度够，计算校验和。
8. CMD 已知 + 长度够 + 校验成功，才认为是合法帧。
9. 合法帧才解析、更新数据、清通信计时器。
10. 校验失败，不清整包，从下一字节继续找 CMD。

这个设计可以兼容串口干扰、前面有垃圾字节、错位接收等情况。

---

### 2.2 发送规则

发送不再等待对应回复。

发送任务每 10ms 调用一次，内部按 100ms 节拍发送一帧：

- 阶段为握手：轮询发送握手命令表。
- 阶段为充电：轮询发送充电命令表。
- 阶段为满电显示：周期发送 B6 显示 SOC。
- 阶段为温度异常等待：轮询温度相关命令。
- 阶段为主动拉低 COM：不再发送协议帧，只保持 COM 低电平。

---

## 3. 通信阶段定义

建议通信层只保留这些阶段：

```c
U1W_STAGE_STOP          // 停止通信，释放 COM
U1W_STAGE_HANDSHAKE     // 握手阶段
U1W_STAGE_CHARGE        // 正常充电通信阶段
U1W_STAGE_FULL_DISPLAY  // 满电后显示 SOC，持续 3 分钟
U1W_STAGE_TEMP_WAIT     // 温度异常等待恢复，继续通信
U1W_STAGE_PULL_LOW      // 主动拉低 COM，通知 BMS 主机结束通信
```

---

## 4. 握手阶段规则

握手阶段必须强制收齐以下帧：

```text
A0 / A1 / A4 / A6 / A7 / B1 / B3 / B4
```

其中 A1 也必须参与握手，不再作为可选帧。

握手发送表：

```c
static const u8 code s_handshake_cmd[] =
{
    U1W_CMD_A0,
    U1W_CMD_A1,
    U1W_CMD_A4,
    U1W_CMD_A6,
    U1W_CMD_A7,
    U1W_CMD_B1,
    U1W_CMD_B3,
    U1W_CMD_B4,
};
```

只有握手帧全部收齐后，`ch.c` 才允许进入后续充电流程。

---

## 5. 充电阶段规则

正常充电阶段每 100ms 发送一帧，循环发送：

```text
B1 / B3 / B4 / B6
```

其中 B6 固定发送“要求打开充电 MOS”：

```text
B6 01 01 CHK
```

设计原则：

- BMS 普通异常由 BMS 自己处理 MOS。
- 充电器不再因为 B4 异常位主动控制 BMS MOS。
- 充电器仍然需要处理自己的异常、通信超时和电池拔出。

---

## 6. 满电阶段规则

进入满电后：

1. 关闭充电输出。
2. 关闭电池电压检测分压，避免持续对电池放电。
3. 通信进入 `U1W_STAGE_FULL_DISPLAY`。
4. 周期发送 B6 显示 SOC。
5. 满电显示持续 3 分钟。
6. 3 分钟后主动拉低 COM。
7. 后续只间歇打开电压检测，判断是否拔电池或是否需要重新握手。

满电后如果电压回落到需要再次充电，不直接回 `CH_Check`，必须重新握手，避免复用旧电池数据。

---

## 7. 异常状态规则

### 7.1 普通异常

普通异常包括但不限于：

- 通信超时
- 关键帧超时
- 充电器过压
- 充电器过流
- 欠压异常
- 充电超时
- 硬件异常

进入普通异常后：

1. 关闭充电输出。
2. 关闭电池电压检测分压。
3. 通信层进入 `U1W_STAGE_PULL_LOW`。
4. 主动拉低 COM，通知 BMS 主机结束通信。
5. 等待电池拔除。
6. 确认拔除后释放 COM，清协议数据，回 `CH_IDLE`。

---

### 7.2 温度异常例外

温度异常不能主动拉低 COM，因为还需要继续通信检查温度是否恢复。

温度异常状态规则：

1. 关闭充电输出。
2. 不拉低 COM。
3. 通信进入 `U1W_STAGE_TEMP_WAIT`。
4. 继续轮询 B3 / B4。
5. 温度恢复后重新握手。
6. 如果电池拔出，清数据并回待机。

温度等待发送表：

```c
static const u8 code s_temp_wait_cmd[] =
{
    U1W_CMD_B3,
    U1W_CMD_B4,
};
```

---

## 8. COM 主动拉低规则

协议中的“总线拉低 30ms”主要是给从机/BMS 判断主机断开使用。

因此主机侧不需要把“COM 被拉低 30ms”作为常规异常判断。

正确做法：

- 满电结束通信后，主机主动拉低 COM。
- 普通异常结束通信后，主机主动拉低 COM。
- 主动拉低 COM 时，不再收发协议帧。
- 主动拉低 COM 不是异常，不应触发 BUS_LOW 错误。

不建议保留主机侧 `BUS_LOW` 异常判断，避免和主动拉低动作冲突。

---

## 9. 通信超时规则

采用两层超时：

### 9.1 总通信超时

任意合法帧都会清零总通信计时。

建议：

```c
U1W_ANY_RX_TIMEOUT_10MS = 200U;  // 2 秒
```

含义：

```text
2 秒内没有收到任何合法帧，判定通信超时。
```

---

### 9.2 关键帧超时

每个关键帧单独有刷新计时。

建议：

```c
U1W_KEY_RX_TIMEOUT_10MS = 300U;  // 3 秒
```

握手阶段关键帧：

```text
A0 / A1 / A4 / A6 / A7 / B1 / B3 / B4
```

充电阶段关键帧：

```text
B1 / B3 / B4
```

温度等待阶段关键帧：

```text
B3 / B4
```

只要关键帧超过 3 秒没有刷新，也判定通信异常，并记录是哪一帧超时。

---

## 10. 电池电压检测分压规则

充电口电压检测会通过分压电阻持续消耗电池电量，因此不能在满电和异常锁止时常开。

### 10.1 正常流程

这些状态可以常开电压检测：

```text
CH_IDLE
BMS_HANDSHAKE
CH_Check
CH_REPAIR
CH_Pre1
CH_CCCV
```

### 10.2 满电和异常锁止

这些状态默认关闭电压检测：

```text
CH_FULL
BMS_ERR
CH_TimOut
CCCV_TimOut
CH_UVP
CH_OVP
CH_OCP
NTC_ERR
HW_ERR
```

间歇检测规则建议：

```text
每 1 秒打开一次电压检测
打开保持 100ms
打开 50ms 后认为 ADC 有效
只在有效窗口内判断 val.vout
判断完自动关闭
```

---

## 11. 电池拔除判断规则

除 `CH_IDLE` 外，每个状态都需要判断电池是否拔出。

拔出判断优先级最高：

```text
拔电池 > 通信超时 > 温度恢复 > 满电回落 > 正常充电判断
```

---

### 11.1 继电器未闭合 / 输出关闭状态

可以用电压判断拔出：

```text
val.vout < vRESET 连续确认
```

满电和异常状态下必须通过间歇电压检测窗口判断，不能用旧 ADC 值直接判断。

---

### 11.2 充电中继电器已闭合状态

充电中继电器闭合时，不能靠电压判断拔电池。

原因：

```text
继电器闭合后，val.vout 可能看到的是充电器自身输出电压，
拔掉电池后电压仍可能存在，容易误判电池还在。
```

充电中拔电池应使用：

```text
输出电流小 + 1 秒无合法通信帧
```

建议规则：

```text
val.curr < iEND / 2
并且 no_rx_10ms >= 100U
再做短时间确认
```

注意优先级：

```text
如果电流小 + 1 秒无通信，优先认为电池拔出，回待机。
如果 2 秒无通信但电流不小，才认为通信异常。
```

---

## 12. ch.c 职责边界

`ch.c` 只负责充电状态机：

- 检测电池插入
- 设置通信阶段
- 判断握手是否完成
- 应用目标电压、电流
- 处理充电器自身异常
- 处理通信超时
- 处理满电、温度异常、拔电池

`ch.c` 不应该：

- 解析 A0/A1/A4/A6/A7/B1/B3/B4/B6 字节内容
- 关心当前发送到哪一帧
- 判断某个协议帧如何组包
- 直接控制协议内部收发细节

---

## 13. uart_1_wire.c 职责边界

`uart_1_wire.c` 只负责一线通信：

- 接收 FIFO 数据
- 模拟接收空闲
- 扫描 CMD
- 校验帧
- 解析合法帧
- 更新协议数据
- 更新握手 mask
- 更新关键帧计时
- 按阶段每 100ms 发送一帧
- 通信超时判断
- 主动拉低/释放 COM

---

## 14. 建议保留的对外接口

```c
void uart_1_wire_init(void);
void uart_1_wire_poll_10ms(void);
void uart_1_wire_set_stage(u8 stage);
void uart_1_wire_get_info(UART_1WIRE_CHARGE_INFO_Types *info);
void uart_1_wire_reset_link(void);
```

不建议恢复这些碎片接口：

```c
uart_1_wire_can_send()
uart_1_wire_is_busy()
uart_1_wire_is_online()
uart_1_wire_is_ready()
uart_1_wire_get_target_voltage_mv()
uart_1_wire_get_target_current_ma()
uart_1_wire_set_charge_enable()
```

原因：这些接口会让 `ch.c` 调用关系变碎，跟踪困难。

---

## 15. 日志规则

协议收发日志默认关闭，避免刷屏。

建议默认：

```c
#define U1W_DEBUG_EN        0U
#define U1W_DEBUG_PARSE_EN  0U
```

正常只保留状态切换日志。

需要现场抓协议时，再临时打开协议日志。

---

## 16. 当前实现文件建议替换

主要替换：

```text
App/uart_1_wire.c
App/uart_1_wire.h
App/ch.c
App/uart_1_wire_debug.c
```

对应版本：

```text
V3：YAT-60V3A-Uart-1-wire_async_poll_simplified_v3.zip
```

---

## 17. 后续打开电脑后的检查顺序

1. Keil Rebuild，先看语法错误。
2. 检查 `TASK_10MS` 是否确实为 10ms。
3. 检查 `BATT_DIVIDER_EN` 极性是否正确。
4. 检查 COM 主动拉低/释放函数方向是否正确。
5. 检查继电器闭合状态是否能准确判断。
6. 检查充电中拔电池逻辑：电流小 + 1 秒无通信。
7. 检查满电 3 分钟 B6 SOC 后是否主动拉低 COM。
8. 检查温度异常是否继续通信，不拉低 COM。
9. 默认关闭协议日志，避免 100ms 收发刷屏。
10. 实机验证握手收齐后才进入充电。

