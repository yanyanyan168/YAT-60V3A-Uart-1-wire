# AGENTS.md - AI 工具协作与编码保护规则

> 本文件放在项目根目录，用于约束 Codex / Cursor / Claude Code / Copilot / 通义灵码等 AI 工具修改本工程代码时的行为。  
> 本规则适用于 Keil、C51、MDK、IAR、Code::Blocks、Source Insight、VS Code、GCC、Makefile、CMake、Python 上位机、手机 APP、网页后台等工程。  
> 开始处理本工程前，必须先读取并遵守项目根目录的 `AGENTS.md` 和 `USER.md`。

---

# 0. 核心目标

本文件的目标不是限制 AI 发挥，而是保证 AI 修改工程时：

1. 不改坏编码；
2. 不破坏老工程风格；
3. 不做无关重构；
4. 不引入不可控依赖；
5. 修改可审查、可回退、可交付。

一句话原则：

```text
能小改，不大改；能说明，不猜测；能验证，不冒险。
```

---

# 1. 默认工作模式

除非用户明确说：

```text
允许写入
```

否则 AI 默认只能执行：

1. 只读查看文件；
2. 分析问题；
3. 给出修改方案；
4. 给出人工合入补丁；
5. 给出需要替换、插入、删除的代码片段。

默认禁止：

1. 写文件；
2. apply patch；
3. 自动格式化；
4. 整文件重写；
5. 批量替换；
6. 自动转码；
7. 自动修复乱码。

---

# 2. Git 工程的写入与备份策略

用户明确补充：

```text
有 Git 管理的工程，不需要每次写入前再额外生成 .bak 备份文件。
```

因此本工程采用以下规则。

## 2.1 如果当前目录是 Git 仓库

AI 在写文件前必须先检查：

```bash
git status --short
git branch --show-current
```

如果满足以下条件：

1. 当前目录是 Git 仓库；
2. 目标文件已经被 Git 跟踪；
3. 工作区干净，或者本次修改范围与用户已有改动不冲突；
4. 用户已经明确说“允许写入”；

则允许直接修改文件，不强制创建 `.bak_encoding_YYYYMMDD_HHMMSS` 备份。

写入后必须输出：

```text
GIT_GUARD_REPORT

- 是否 Git 仓库：
- 当前分支：
- 修改文件：
- 写入前工作区状态：
- 写入后工作区状态：
- 建议用户查看：git diff
- 回退方式：git checkout -- <file> 或 git restore <file>
```

## 2.2 如果 Git 工作区不干净

如果目标文件本来就有用户未提交改动，AI 必须谨慎。

允许行为：

1. 只改本任务直接相关的最小范围；
2. 修改前说明检测到未提交改动；
3. 修改后提醒用户用 `git diff` 核对；
4. 不覆盖用户已有改动。

如果无法保证不覆盖用户已有改动，必须停止：

```text
GIT_GUARD_STOP: 目标文件已有未提交改动，无法保证不覆盖用户修改，未写入文件。
```

## 2.3 如果不是 Git 仓库

如果当前目录不是 Git 仓库，且用户允许写入，则写入编码敏感文件前必须创建备份。

备份文件命名格式：

```text
原文件名.bak_encoding_YYYYMMDD_HHMMSS
```

示例：

```text
main.c.bak_encoding_20260511_213000
uart.c.bak_encoding_20260511_213000
protocol.h.bak_encoding_20260511_213000
```

## 2.4 如果目标文件未被 Git 跟踪

即使当前目录是 Git 仓库，如果目标文件是未跟踪文件，也不能把 Git 当成备份。

处理方式：

1. 优先提醒用户该文件未被 Git 跟踪；
2. 如果要直接写入，应先备份；
3. 或建议用户先执行 `git add <file>` 后再继续。

---

# 3. 编码保护最高原则

编码保护优先级高于功能修改。

如果编码保护和功能修改冲突，必须放弃功能修改，先保护编码。

如果无法确认编码安全，唯一允许的行为是停止并输出人工合入补丁：

```text
ENCODING_GUARD_STOP: 无法保证编码不变，未修改文件。
```

---

# 4. 编码敏感文件类型

以下文件默认视为编码敏感文件：

```text
.c
.h
.hpp
.cpp
.cc
.s
.S
.asm
.inc
.a51
.lst
.map
.txt
.md
.ini
.cfg
.bat
.cmd
.mak
makefile
Makefile
CMakeLists.txt
.uvproj
.uvprojx
.uvopt
.uvoptx
.ewp
.eww
.project
.py
.spec
.js
.ts
.tsx
.jsx
.json
.yaml
.yml
```

只要文件中可能含有中文注释、中文字符串、中文日志，就必须按编码敏感文件处理。

---

# 5. 修改前只读检查

在修改任何编码敏感文件前，必须先做只读检查。

只读检查阶段禁止写文件，禁止 apply patch，禁止格式化，禁止保存。

必须输出：

```text
ENCODING_GUARD_PRECHECK

- 文件路径：
- 文件大小：
- 是否存在 BOM：
- 推测编码：
- 行尾格式：CRLF / LF / mixed / unknown
- 是否混合行尾：
- 是否包含无法按 UTF-8 解码的字节：
- 是否包含中文：
- 是否包含 emoji：
- 是否包含非 GBK/CP936 可表示字符：
- 是否存在疑似乱码文本：
- 是否 Git 跟踪：
- 是否需要额外备份：
- 是否允许安全写入：是 / 否
```

如果文件疑似 GBK / CP936 / ANSI，必须输出：

```text
ENCODING_GUARD: 该文件疑似 GBK/CP936/ANSI，禁止使用普通 UTF-8 写文件方式。
```

如果无法判断编码，必须停止：

```text
ENCODING_GUARD_STOP: 无法确认原始编码，未修改文件。
```

---

# 6. 编码判断要求

不能只看文件扩展名判断编码。

必须综合判断：

1. 是否有 BOM；
2. 是否能被 UTF-8 严格解码；
3. 是否能被 GBK / CP936 解码；
4. 文件中中文是否可读；
5. 是否存在典型乱码；
6. 文件历史和工程类型；
7. 当前编辑器和编译器环境。

常见判断规则：

```text
有 UTF-8 BOM：
    可以认为是 UTF-8 BOM，但仍需确认工程是否允许。

无 BOM，UTF-8 严格解码成功：
    只能说明“可能是 UTF-8”，不能直接整文件重写。

无 BOM，UTF-8 严格解码失败，但 GBK/CP936 可解码：
    高概率是 GBK/CP936/ANSI。

GBK 和 UTF-8 都能解码：
    必须结合中文显示效果判断，不能擅自转换。

文件中没有中文：
    仍需保持原字节风格、原 BOM 状态和原行尾格式。
```

---

# 7. 绝对禁止项

以下行为禁止执行：

1. 禁止擅自把源码文件转换成 UTF-8。
2. 禁止擅自把源码文件转换成 UTF-8 BOM。
3. 禁止擅自把源码文件转换成 UTF-16。
4. 禁止整文件重写。
5. 禁止无关格式化。
6. 禁止批量格式化整个文件或整个目录。
7. 禁止跨大段正则替换。
8. 禁止自动整理 include。
9. 禁止自动重排函数。
10. 禁止自动删除空行。
11. 禁止自动转换 Tab 和空格。
12. 禁止修改行尾格式。
13. 禁止修改文件末尾是否带换行的状态。
14. 禁止新增 emoji。
15. 禁止新增特殊图标字符。
16. 禁止新增原编码不可表示的字符。
17. 禁止为了“修复乱码”而自动转码。
18. 禁止顺手优化与任务无关的代码。
19. 禁止顺手润色与任务无关的文案。
20. 禁止无依据改动协议、阈值、保护逻辑。

说明：

```text
如果工程已经由 Git 管理，并且满足第 2 节条件，则“不生成额外 .bak 备份”不算违反本规则。
```

---

# 8. 唯一允许的安全写入流程

如果用户明确允许写入，并且编码已确认，必须按下面流程执行：

1. 用二进制方式读取原始文件字节。
2. 记录原始文件哈希。
3. 检测并记录 BOM。
4. 检测并记录行尾格式。
5. 按原始编码解码。
6. 只修改和任务直接相关的最小行范围。
7. 新增内容必须能被原编码编码。
8. 按同一原始编码重新编码。
9. 保持原始 BOM 状态。
10. 保持原始行尾格式。
11. 写入临时文件。
12. 用原编码重新读取临时文件验证。
13. 对比差异，确认只有预期修改。
14. 替换原文件。
15. 再次读取验证。
16. 输出编码保护报告。
17. 如果是 Git 仓库，输出 Git 差异查看建议。

---

# 9. GBK / CP936 / ANSI 文件写入要求

如果目标文件是 GBK / CP936 / ANSI：

1. 必须使用 `gbk` 或 `cp936` 解码。
2. 必须使用同一编码写回。
3. 禁止使用默认编码。
4. 禁止使用 UTF-8 写回。
5. 禁止新增 emoji。
6. 禁止新增 GBK 不能表示的字符。
7. 新增中文注释必须是普通简体中文。
8. 新增日志建议使用 ASCII 标签加普通中文。
9. 标点尽量使用 ASCII 标点。
10. 不确定字符能否表示时，必须先停止。

推荐：

```c
usr_printf("[UART] 接收超时\r\n");
usr_printf("[BMS] 等待握手成功\r\n");
usr_printf("[CHG] 进入恒流恒压阶段\r\n");
```

不推荐：

```c
usr_printf("【UART】接收超时\r\n");
usr_printf("→ 等待握手成功\r\n");
usr_printf("● 进入恒流恒压阶段\r\n");
```

禁止：

```c
usr_printf("✅ UART接收成功\r\n");
usr_printf("🎬 进入充电流程\r\n");
usr_printf("⚠️ 电池温度异常\r\n");
```

---

# 10. UTF-8 文件写入要求

如果目标文件确认是 UTF-8：

1. 也不能整文件重写。
2. 也不能无关格式化。
3. 也不能新增 emoji，除非用户明确允许。
4. 也必须保持原行尾。
5. 也必须保持 BOM 状态。
6. 如果工程是嵌入式老工程，即使文件是 UTF-8，也建议日志中只使用 ASCII 标签和普通中文。

---

# 11. 文案、注释、日志保护规则

禁止自作主张“优化”文案。

除非用户明确要求修改文字表达，否则不得修改：

1. 中文注释；
2. 英文注释；
3. 调试日志；
4. printf / usr_printf / LOG 输出文本；
5. 错误提示文本；
6. 状态说明文本；
7. 文档说明文字；
8. README / MD 文档内容；
9. 用户原有命名习惯；
10. 用户原有描述风格。

禁止因为以下理由擅自改文案：

1. 看起来更通顺；
2. 看起来更专业；
3. 看起来更简洁；
4. 看起来更统一；
5. 看起来更符合 AI 的表达习惯；
6. 顺手整理；
7. 顺手润色；
8. 顺手规范化。

允许修改文案的情况：

1. 用户明确要求修改该处文案；
2. 原文案导致编译错误；
3. 原文案导致编码风险；
4. 原文案与本次代码逻辑修改强相关，不改会造成明显误导。

源码、注释、日志、字符串中禁止新增：

```text
emoji
特殊图标
不可见控制字符
不确定编码兼容性的符号
非工程必要的 Unicode 符号
```

推荐日志标签只使用 ASCII 字符：

```text
[INIT]
[OK]
[ERR]
[WARN]
[INFO]
[ADC]
[UART]
[CAN]
[BMS]
[CHG]
[PRE]
[CCCV]
[FULL]
[TEST]
```

---

# 12. 最小修改原则

修改代码时必须遵守：

1. 只改本任务直接相关代码。
2. 修改范围越小越好。
3. 能改 1 行，不改 3 行。
4. 能改 1 个函数，不改整个文件。
5. 不改无关变量名。
6. 不改无关注释。
7. 不调整无关缩进。
8. 不改变代码风格。
9. 不引入新的编码风险。
10. 不做顺手优化。
11. 不擅自修改协议字段、大小端、阈值、保护策略。
12. 不擅自改变原有状态机跳转条件。

禁止因为“看起来更整洁”而扩大修改范围。

---

# 13. 嵌入式 C 工程规则

处理 MCU / C51 / STM32 / PY32 / PIC / AVR / GD32 / NXP 等工程时，默认遵守：

1. 优先兼容 C89/C90。
2. 变量声明尽量放在代码块开头。
3. 默认不使用 `malloc/free`。
4. 默认不使用 `float/double`。
5. 默认不引入大型库。
6. 默认不使用递归。
7. 中断中只做必要动作。
8. 通信接收优先中断 + FIFO。
9. 主循环或周期任务处理复杂逻辑。
10. 状态机跳转必须有明确条件。
11. 协议解析必须明确大小端、长度、校验、超时。
12. ADC / 滤波 / 控制算法优先整数化。

---

# 14. 电脑端串口调试工具规则

处理 Python / PySide6 / PyQt / Tkinter / C# / Electron 等上位机工具时，默认遵守：

1. 串口接收不能卡 UI。
2. 长任务放线程或异步处理。
3. UI 操作必须有明确反馈。
4. 日志、导出、缓存大小要清楚。
5. CSV 导出字段要稳定。
6. 配置项应可保存和恢复。
7. 打包前说明依赖和 PyInstaller 注意事项。
8. 修改 UI 时不要只追求好看，必须适合测试人员操作。
9. 对串口协议要保留原始帧显示和解析结果。
10. 异常断开、端口占用、权限不足要有提示。

---

# 15. 手机端设备 APP 规则

处理 Android / iOS / React Native / Flutter / Expo 等手机端应用时，默认遵守：

1. 这是设备配套 APP，不是普通互联网 APP。
2. 优先保证连接稳定、提示清楚、流程可靠。
3. 蓝牙 / 串口 / 网络通信必须考虑断连、重连、超时。
4. 每次连接成功后，如项目需要，应同步设备时间。
5. 参数配置必须有确认、保存、失败提示。
6. 测试结果、历史记录、导出流程要清楚。
7. UI 适合现场测试人员，不要为了炫酷牺牲稳定性。
8. 协议字段不要凭空编造，必须以项目文档或源码为准。

---

# 16. 文档与交付规则

用户经常需要把内容交给老板、客户或同事。

输出文档时：

1. 优先使用 Markdown。
2. 结构清晰，标题分级明确。
3. 先结论，再原因，再证据，再方案。
4. 对老板版本要少讲废话，突出结论和解决方案。
5. 对客户版本要有事实依据，避免情绪化。
6. 对交接文档要写清楚路径、账号、配置、当前进度、风险点、后续目标。
7. 不确定的信息必须标注“需核对”。

---

# 17. 修改后必须复查

修改后必须输出：

```text
ENCODING_GUARD_REPORT

- 文件：
- 原编码：
- 写回编码：
- 原 BOM：
- 写回 BOM：
- 原行尾：
- 写回行尾：
- 是否混合行尾：
- 是否新增非 ASCII 字符：
- 是否新增非 GBK 字符：
- 是否包含 emoji：
- 是否发现乱码：
- 是否需要额外备份：
- 备份文件：
- 修改行范围：
- 是否只做最小修改：
```

如果是 Git 仓库，还必须输出：

```text
GIT_GUARD_REPORT

- 当前分支：
- 修改文件：
- 写入前状态：
- 写入后状态：
- 建议检查命令：git diff
- 回退命令：git restore <file>
```

并贴出关键修改前后片段。

---

# 18. 发现乱码后的处理

如果修改后发现乱码风险，必须立即：

1. 停止继续修改；
2. 如果有备份，从备份恢复；
3. 如果是 Git 仓库，提示使用 Git 回退；
4. 不得尝试自动修复；
5. 不得继续猜测编码；
6. 输出回滚报告。

回滚报告格式：

```text
ENCODING_GUARD_ROLLBACK

- 文件：
- 原因：
- 是否已恢复：
- 恢复方式：
- 保留的修改：
- 当前状态：
```

如果无法回滚，必须明确报告：

```text
ENCODING_GUARD_CRITICAL: 检测到乱码风险，但无法自动回滚，请立即人工检查备份或 Git 差异。
```

---

# 19. 工具能力不足时的唯一行为

如果当前 AI 工具、编辑器、脚本环境无法做到以下任一项：

1. 检测原始编码；
2. 识别 BOM；
3. 保持原编码写回；
4. 保持原行尾；
5. 只做最小行级修改；
6. 写回后用原编码验证；
7. 检查 Git 状态或创建备份；
8. 差异复查；

则禁止直接修改文件。

必须输出：

```text
ENCODING_GUARD_STOP: 当前工具无法保证编码不变，因此未修改文件。下面仅提供人工合入补丁。
```

---

# 20. 人工合入补丁格式

如果不能安全写入文件，必须输出方便人工定位的补丁。

核心原则：

```text
行号只能作为辅助定位，必须同时提供文件路径、函数名/结构体名/模块位置、原始行号范围和原代码片段，避免文件改动后行号漂移导致误合入。
```

模板：

```text
文件：xxx.c

修改 1：
- 函数/位置：
- 原始行号：
- 修改类型：替换 / 插入 / 删除
- 定位关键字：

定位原代码：
```

```c
// Lxxx
原代码片段
```

```text
替换为 / 插入内容 / 删除内容：
```

```c
// Lxxx
新代码片段
```

```text
说明：
- 修改原因：
- 修改范围：
- 是否包含中文：
- 是否包含非 ASCII：
- 是否包含 emoji：
- 是否需要按原编码保存：
- 是否需要保持原行尾：
- 是否需要人工复查：
```

强制要求：

1. 有文件路径。
2. 有函数名或模块位置。
3. 有修改前原始行号范围。
4. 有原代码定位片段。
5. 有新代码片段。
6. 有修改原因。
7. 有编码风险说明。
8. 有是否包含中文 / 非 ASCII / emoji 的说明。
9. 有是否需要按原编码保存的说明。
10. 有是否保持原行尾的说明。

---


# 21. 推荐源码日志规范

为了兼容老工程，建议统一采用 ASCII 标签 + 普通中文：

```c
usr_printf("[INIT] 系统初始化完成\r\n");
usr_printf("[UART] 接收超时\r\n");
usr_printf("[ADC] 采样异常\r\n");
usr_printf("[BMS] 通信失败\r\n");
usr_printf("[CHG] 进入预充阶段\r\n");
usr_printf("[ERR] 输出过压\r\n");
```

不建议使用：

```c
usr_printf("【初始化】系统初始化完成\r\n");
usr_printf("-----> 进入预充阶段\r\n");
usr_printf("※ 通信失败\r\n");
usr_printf("● 输出过压\r\n");
```

禁止使用：

```c
usr_printf("🎬 系统初始化完成\r\n");
usr_printf("✅ 通信成功\r\n");
usr_printf("❌ 通信失败\r\n");
usr_printf("⚠️ 输出过压\r\n");
```

---

# 22. 建议的工程约定

如果是 Windows 老嵌入式工程，建议在项目说明中明确：

```text
源码编码：保持原文件编码，不统一转码。
老文件：多数为 GBK/CP936/ANSI。
新文件：由项目负责人决定编码。
行尾：保持原文件行尾。
中文注释：允许，但必须保持原编码可表示。
中文日志：允许，但禁止 emoji 和特殊图标。
AI 修改：默认只输出补丁，不直接写文件。
Git 工程：允许写入后以 git diff 审查，不强制额外 .bak。
非 Git 工程：写入前必须备份。
```

如果项目决定统一 UTF-8，必须一次性规划迁移，不允许 AI 工具边改边混合转码。

---

# 23. 最硬的一句话

当非常担心 AI 工具改坏编码时，直接加这句：

```text
除非你能证明“读取编码、写回编码、BOM、行尾、修改范围、Git 状态或备份策略”全部可控，否则禁止修改文件，只能给我人工合入补丁。
```

更硬一点：

```text
如果你调用了任何可能整文件重写、自动保存、默认 UTF-8 写入、自动格式化、覆盖用户未提交改动的工具，本次任务视为失败。你必须停止并报告，不得继续。
```

---

# 24. 总结

编码敏感工程的核心原则：

```text
先识别，再修改；
有 Git，看 diff；
无 Git，先备份；
先保护编码，再修改功能；
能小改，不大改；
能给补丁，不乱写；
不能证明安全，就停止。
```

不要相信“默认保存”。  
不要相信“自动修复”。  
不要相信“看起来没问题”。  
必须以原始字节、原始编码、原始行尾、Git 差异、最小修改为准。

---

# 25. CodeGraph / 代码图谱使用规则

如果项目已经安装或启用了 CodeGraph，AI 工具在分析和修改代码前应优先使用 CodeGraph 做代码关系分析。

CodeGraph 的定位：

```text
CodeGraph 不是替代 Codex / Cursor / Claude Code。
CodeGraph 是给 AI 工具提供代码地图，用于查函数、变量、调用链、影响范围和上下文。
```

## 25.1 使用前状态检查规则

AI 工具每次使用 CodeGraph 前，必须先在项目根目录检查 CodeGraph 当前状态。

推荐顺序：

```bash
codegraph status
```

如果项目还没有初始化 CodeGraph，应初始化：

```bash
codegraph init -i
```

初始化完成后，必须再次查看状态：

```bash
codegraph status
```

如果 CodeGraph 状态不是最新，或者提示索引过期、文件有变化、需要同步，应先同步：

```bash
codegraph sync
```

同步完成后，必须再次查看状态：

```bash
codegraph status
```

确认 CodeGraph 可用并且状态正常后，才继续查询函数、变量、调用链和影响范围。

说明：

```text
CodeGraph 索引是辅助分析文件，不是源码。
是否提交 .codegraph 到 Git，由项目负责人决定。
即使 CodeGraph 已同步，最终结论仍必须以源码和 git diff 为准。
```


## 25.2 修改代码前必须优先做 CodeGraph 分析

涉及源码分析、BUG 定位、功能修改、重构建议时，AI 应优先使用 CodeGraph 查询：

1. 目标函数在哪里定义；
2. 谁调用了目标函数；
3. 目标函数内部调用了谁；
4. 相关全局变量在哪里定义；
5. 相关全局变量在哪里读写；
6. 目标模块与其他模块的依赖关系；
7. 修改可能影响哪些文件和函数；
8. 是否涉及中断、定时器、通信、状态机、ADC、PWM、显示、存储等关键路径。

禁止在没有确认调用链和影响范围前直接修改关键代码。

## 25.3 推荐的 CodeGraph 分析顺序

AI 工具应按以下顺序处理复杂代码问题：

```text
1. 用 CodeGraph 搜索目标符号；
2. 查看目标函数定义位置；
3. 查看 callers，确认上游调用关系；
4. 查看 callees，确认下游依赖；
5. 查看相关全局变量的读写位置；
6. 查看相关文件和模块关系；
7. 输出影响范围分析；
8. 再读取具体源码片段；
9. 给出最小修改方案；
10. 用户允许写入后，才按 ENCODING_GUARD + GIT_GUARD 修改。
```

## 25.4 必须输出的 CodeGraph 分析报告

在修改重要代码前，应先输出：

```text
CODEGRAPH_ANALYSIS_REPORT

- 是否检测到 .codegraph：
- 目标函数 / 变量：
- 定义位置：
- 上游调用者：
- 下游被调用函数：
- 相关全局变量：
- 读写位置：
- 涉及文件：
- 可能影响的模块：
- 是否涉及 ISR / 定时器：
- 是否涉及通信协议：
- 是否涉及状态机：
- 是否涉及硬件控制：
- 风险点：
- 建议修改范围：
```

如果 CodeGraph 查询结果不完整，必须明确说明：

```text
CODEGRAPH_WARNING: CodeGraph 结果不完整，下面结论需要结合源码人工复核。
```

## 25.5 嵌入式项目中的重点检查项

处理 MCU / C51 / STM32 / PY32 / PIC / AVR / GD32 / NXP 等工程时，CodeGraph 分析应重点检查：

1. 中断入口函数；
2. 定时器周期任务；
3. 主循环任务调度；
4. 状态机入口和状态切换；
5. UART / CAN / I2C / SPI 接收和发送流程；
6. FIFO 写入和读取位置；
7. ADC 采样、滤波和工程单位换算；
8. PWM / GPIO / 继电器 / MOS 控制函数；
9. 保护逻辑，例如过压、欠压、过流、过温、通信超时；
10. 全局变量是否在 ISR 和主循环之间共享。

## 25.6 不允许把 CodeGraph 当成最终依据

CodeGraph 只能作为辅助索引，不能替代源码复核。

以下情况必须人工复核源码：

1. 宏展开复杂；
2. 条件编译复杂；
3. Keil / C51 特殊关键字较多；
4. 函数指针、回调、表驱动较多；
5. 中断入口由启动文件或编译器关键字绑定；
6. 代码通过宏拼接函数名或变量名；
7. 老工程存在重复文件、备份文件、不同版本文件。

如果 CodeGraph 与源码不一致，以源码为准。

## 25.7 禁止行为

使用 CodeGraph 时仍然禁止：

1. 未经用户允许初始化项目；
2. 未经用户允许写入 `.codegraph`；
3. 只凭调用图直接改代码；
4. 忽略 ENCODING_GUARD；
5. 忽略 GIT_GUARD；
6. 为了让 CodeGraph 解析成功而擅自修改源码；
7. 为了索引方便而批量重命名文件或函数；
8. 为了索引方便而改 include、宏定义、工程结构；
9. 把 CodeGraph 的猜测结果当成协议或硬件事实。


## 25.9 一句话原则

```text
先用 CodeGraph 看清战场，再用 Codex 修改代码；CodeGraph 负责找关系，Codex 负责分析和改代码，最终仍以源码和 git diff 为准。
```

---

# 26. 小范围修改的高效执行规则

本章节适用于所有“只改一个函数、只删一个 helper、只补一个判断、只改一个宏”的小任务。

## 26.1 先限定范围，再动代码

小范围任务禁止直接全文件搜索替换。

必须先确认：

1. 目标文件；
2. 目标函数；
3. 目标函数起止边界；
4. 同名 case、同名宏、同名变量是否也出现在其他函数里。

例如同一个 `case U1W_CMD_A0:` 可能同时出现在：

1. `u1w_reply_len()`；
2. `u1w_parse_frame()`；
3. 调试打印函数；
4. 其他协议辅助函数。

如果任务只要求改 `u1w_parse_frame()`，就只能在这个函数范围内改，不能全文件按 `case U1W_CMD_A0:` 插入或替换。

## 26.2 禁止低效命令堆叠

小改动不允许为了显得“做了很多”而反复跑无关命令。

推荐最小流程：

```text
1. git status / show_changes，看当前工作区和目标文件 diff；
2. 读取目标函数片段；
3. 做编码预检；
4. 只改目标函数或目标小块；
5. 复读目标函数片段；
6. 搜索旧符号确认没有残留；
7. 运行必要编译；
8. 输出简短报告。
```

如果中间发现命令方式不适合，例如 UTF-8 apply_patch 不能处理 CP936 文件，应立即切换到保编码方式，不要反复尝试可能破坏编码的方法。

## 26.3 CP936 / GBK 老工程写入规则

如果预检发现 `.c/.h/.txt/.md` 等文件是 CP936 / GBK / ANSI：

1. 不要用默认 UTF-8 方式直接写；
2. 不要整文件转码；
3. 不要为了修改一小块而重写整文件；
4. 应使用保留原编码、原 BOM、原行尾的精确替换或按行修改；
5. 写入后必须复查编码、BOM、行尾。

如果无法确认保编码写入，必须停止并给人工补丁。

## 26.4 计划外问题处理规则

执行 current-plan.md 时，如果发现计划外 BUG、可疑问题、顺手优化点：

1. 不允许自己修改；
2. 只允许记录到 `.ai-bridge/agent-status.md` 的 `Found but not changed`；
3. 停下来等 ChatGPT Pro / 用户确认；
4. 等 ChatGPT Pro 给出具体最小改法后才能继续。

禁止“顺手修一下”。

## 26.5 交付 diff 规则

`implementation-diff.patch` 必须对应本轮任务。

如果本轮只改了一个文件，patch 就只应包含这个文件和必要的状态文件。禁止把旧任务的大 diff、历史 `ch.c` diff、无关日志文件混入本轮 patch。

生成 patch 前必须确认：

```text
1. 本轮实际改了哪些文件；
2. 哪些是历史未提交改动；
3. 哪些不属于本轮；
4. implementation-diff.patch 是否只包含本轮相关内容。
```

## 26.6 报告规则

最终报告只写结论，不贴流水账。

必须包含：

1. 改了哪些文件；
2. 没改哪些禁止文件；
3. 核心逻辑是否满足计划；
4. 编译结果；
5. Program Size；
6. 回退方式；
7. 计划外问题是否有记录。

不要把每一条命令过程都复制给用户，除非用户要求排查过程。

---

# 27. GBK/CP936 中文源码读取与保编码修改技能

本技能适用于 Keil C51 老工程中的 `.c/.h/.txt/.md/.ini/.uvproj` 等文本文件，特别是含中文注释、中文串口日志、GBK/ANSI 编码的文件。

## 27.1 先判断编码，不要默认 UTF-8

读取中文乱码时，不要立刻认为源码乱码。

必须先做编码判断：

```text
1. 读取原始 bytes；
2. 依次尝试 utf-8-sig、utf-8、gbk、cp936；
3. 哪个能完整 decode，就记录为原编码；
4. 如果 UTF-8 读取显示乱码，但 GBK/CP936 正常，说明文件本身没坏，是读取方式错了。
```

推荐检查命令：

```text
PYTHONIOENCODING=utf-8 python -c "from pathlib import Path
for f in ['App/usr_cfg.h','App/ch.c','App/uart_1_wire.c']:
    b=Path(f).read_bytes()
    ok=[]
    for enc in ['utf-8-sig','utf-8','gbk','cp936']:
        try:
            b.decode(enc)
            ok.append(enc)
        except Exception:
            pass
    print(f, ok, 'bytes', len(b))"
```

## 27.2 GBK 文件读取中文时，要同时指定输出编码

在 Windows / MSYS / Codex 工具中，即使 Python 已经用 GBK 正确 decode，如果控制台输出编码不对，显示仍可能乱码。

读取 GBK 中文源码时，推荐使用：

```text
PYTHONIOENCODING=utf-8 python -c "from pathlib import Path
p=Path('App/ch.c')
lines=p.read_bytes().decode('gbk', errors='replace').splitlines()
for i in range(1, 80):
    print(f'{i:4d} | {lines[i-1]}')"
```

关键点：

```text
1. 文件 decode 用 gbk/cp936；
2. 控制台输出用 PYTHONIOENCODING=utf-8；
3. 不要用默认 read 工具的 UTF-8 结果判断中文是否损坏。
```

## 27.3 修改 GBK 文件必须保留三件事

修改前必须记录：

```text
1. 原编码：GBK/CP936/ANSI/UTF-8/UTF-8-SIG；
2. 原 BOM：有 / 无；
3. 原行尾：CRLF / LF。
```

修改后必须保证：

```text
1. 写回相同编码；
2. 写回相同行尾；
3. 不新增或删除 BOM；
4. 不整文件转码；
5. 不批量格式化。
```

对于 GBK 文件，推荐做法是：

```text
b = path.read_bytes()
text = b.decode('gbk')
text = text.replace(old, new, 1)
path.write_bytes(text.encode('gbk'))
```

禁止做法：

```text
path.write_text(text, encoding='utf-8')
```

## 27.4 修改后必须复查中文和编码

修改后至少检查：

```text
1. 用 GBK/CP936 能否重新 decode；
2. 文件是否仍然无 BOM / 原 BOM；
3. 行尾是否仍为 CRLF；
4. 中文注释和中文日志是否能按 GBK 正常显示；
5. git diff 是否只包含计划内小范围修改。
```

## 27.5 处理 Keil C51 中文字符串里的 0xFD 风险

部分 GBK 汉字本身含 `0xFD` 字节，例如：

```text
待 = B4 FD
过 = B9 FD
数 = CA FD
```

如果运行时串口日志中遇到这类字，可能被历史代码用 `\xFD` 规避，但这会导致日志里多出异常字节。

优先方案不是硬塞 `\xFD`，而是换词避开这些字：

```text
等待BMS握手 -> 等BMS握手
过压保护   -> 高压保护
过温保护   -> 高温保护
过流保护   -> 电流保护
过流恢复   -> 电流恢复
放大倍数   -> GAIN
```

中文注释可以保留这些字；运行时字符串尽量避开含 `0xFD` 的汉字。

## 27.6 一句话原则

```text
先用 bytes 判断编码，再用正确编码读中文；改 GBK 文件必须 GBK 写回，不能默认 UTF-8。
```

