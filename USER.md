# USER.md - 用户画像与协作偏好

> 本文件放在项目根目录，用于告诉 AI 代码助手：用户是谁、偏好什么、代码应该按什么风格输出。  
> 本文件不绑定某一个具体项目，适用于嵌入式 C / MCU / 老工程维护 / 电脑端串口调试工具 / 手机端设备 APP / 本地 AI 辅助开发场景。

---

# 一、基础画像

用户主要从事嵌入式软件开发，偏向 MCU 裸机、小型 MCU、量产型固件和老工程维护。

同时，用户并不只写单片机程序。根据项目需要，用户也会开发：

1. 电脑端串口调试工具；
2. 串口波形记录和数据导出工具；
3. PC 端测试辅助软件；
4. 手机端设备配套 APP；
5. 蓝牙 APP / 参数配置 APP / 数据同步 APP；
6. 项目交接文档、客户回复文档、老板汇报文档。

用户常见工作链路是：

```text
MCU 固件
+ 通信协议
+ PC 串口调试工具
+ 手机 APP
+ 测试流程
+ 交付文档
```

默认语言：简体中文。

---

# 二、常见工作内容

用户常见工作内容包括：

1. MCU 外设初始化与驱动开发；
2. 充电器 / BMS / 电源控制类固件；
3. 气体检测仪 / 酒驾测试仪 / 检测设备固件；
4. 串口、CAN、I2C、SPI、ADC、PWM、定时器、中断等底层模块；
5. 通信协议解析、状态机设计、故障保护逻辑；
6. 老 Keil / Code::Blocks / C51 / MDK / IAR / Source Insight 工程维护；
7. Python / PySide6 / PyQt / Tkinter 串口调试工具；
8. CSV 导出、曲线显示、波形记录、A/B 游标、测试记录；
9. Android / iOS / React Native / Flutter / Expo 设备配套应用；
10. 本地知识库、AI 辅助代码分析、代码重构和调试；
11. Markdown 技术方案、交接文档、客户说明、老板汇报材料。

---

# 三、回答语言与表达风格

AI 回答时应遵守：

1. 默认使用简体中文。
2. 不要使用英文长篇解释，除非术语必须保留英文。
3. 优先给工程可落地方案，不要只讲概念。
4. 不要过度炫技，不要为了“高级”而复杂化。
5. 对嵌入式问题，优先从稳定性、可维护性、量产风险角度分析。
6. 对上位机工具问题，优先从稳定、不卡界面、好操作、可打包角度分析。
7. 对手机 APP 问题，优先从连接稳定、提示清楚、现场可用角度分析。
8. 遇到代码问题，应先指出风险点，再给修改方案。
9. 遇到不确定内容，要明确说明不确定，不要编造。
10. 用户有时需要详细分析，有时会要求“不要啰嗦”；如果用户说简洁，就必须简洁。

推荐回答顺序：

```text
先结论；
再原因；
再风险；
再给可直接使用的代码或修改片段。
```

---

# 四、代码风格偏好

用户偏好稳定、保守、适合量产的代码。

默认要求：

1. 嵌入式优先使用 C 语言。
2. 嵌入式优先裸机思路。
3. 默认不使用动态内存。
4. 默认不使用 `malloc/free`。
5. 默认不使用 `float/double`。
6. 优先使用定点数、整数比例、放大倍数计算。
7. 优先使用状态机、周期任务、非阻塞流程。
8. 避免长时间阻塞延时。
9. 中断里只做必要动作，例如置标志、收发 FIFO、计数。
10. 复杂逻辑尽量放到主循环或周期任务中处理。
11. 代码应便于 Keil / Code::Blocks / IAR / GCC 等常见嵌入式环境移植。
12. 上位机和 APP 代码也要重视可维护性，不要只追求界面好看。

---

# 五、常用单位约定

除非用户或项目明确指定，否则默认使用以下单位：

```text
电压：mV
电流：mA
功率：mW 或 W，按上下文明确
温度：0.1°C 可用 xxx_x10 表示
时间：ms
周期任务：常见 10ms tick
容量：mAh
百分比：0~100
比例：常用 x10 / x100 / x1000 / x10000
```

变量命名建议体现单位，例如：

```c
uint16_t bat_vol_mv;
uint16_t chg_cur_ma;
int16_t  temp_x10;
uint32_t timeout_ms;
uint16_t factor_x1000;
```

---

# 六、嵌入式工程默认限制

在没有用户明确允许前，AI 不应默认引入：

1. 操作系统依赖；
2. 大型库；
3. 动态内存；
4. 浮点计算；
5. 递归；
6. 复杂模板或宏魔法；
7. 不适合小 MCU 的大表；
8. 大量 printf；
9. 过度封装；
10. 需要复杂运行环境的方案。

如果必须引入，应先说明原因和资源代价。

---

# 七、电脑端串口调试工具偏好

用户有时会写电脑端串口调试工具。

常见技术方向：

1. Python；
2. PySide6 / PyQt；
3. Tkinter；
4. pyserial；
5. pyqtgraph / matplotlib；
6. CSV 导出；
7. PyInstaller 打包。

AI 处理这类项目时应注意：

1. 串口接收不能卡 UI；
2. 接收线程、解析线程、UI 刷新要分清；
3. 大量数据记录要考虑缓存上限；
4. 波形显示要考虑降采样和刷新频率；
5. CSV 导出字段要稳定，便于 Excel 打开；
6. 操作按钮要有明确反馈；
7. 测试人员能看懂，不要设计得太复杂；
8. 打包时要说明依赖、入口文件、资源文件和常见警告；
9. 修改 UI 时要尽量给完整文件或完整函数；
10. 如果用户要求“给完整文件覆盖”，应尽量满足。

---

# 八、手机端设备 APP 偏好

用户有时会写手机端应用，通常是设备配套 APP，不是普通互联网 APP。

常见方向：

1. 蓝牙连接；
2. 串口透传或自定义协议；
3. 设备参数配置；
4. 测试流程引导；
5. 测试结果显示；
6. 历史记录；
7. 时间同步；
8. 数据导出；
9. 设备状态提示。

AI 处理这类项目时应注意：

1. 连接成功、连接失败、断连、重连都要有明确提示；
2. 每次连接成功后，如果项目需要，应强制同步设备时间；
3. 协议收发要有超时和错误提示；
4. 页面流程要适合现场测试人员；
5. 不要为了炫酷牺牲稳定性；
6. 不要凭空编造协议字段；
7. 修改前应先理解现有页面、store、protocol、types 的关系；
8. UI 优化要同时考虑数据流和通信流程；
9. APP 文案要清楚，但不要擅自改动项目既有术语。

---

# 九、代码注释要求

用户喜欢完整、清晰、工程化的中文注释。

注释要求：

1. 关键逻辑必须有中文注释。
2. 状态机跳转原因必须写清楚。
3. 超时、计数、滤波、阈值判断必须说明意图。
4. 不要写废话注释。
5. 不要只翻译代码表面意思。
6. 注释应解释“为什么这样做”。

推荐：

```c
/* 连续 20 次满足条件后再切换状态，避免 ADC 抖动导致误判 */
if(++stable_cnt >= 20)
{
    stable_cnt = 20;
    chg_state = CH_CCCV;
}
```

不推荐：

```c
/* cnt 加 1 */
cnt++;
```

---

# 十、日志风格要求

嵌入式源码中的日志应兼容老工程编码。

推荐使用 ASCII 标签 + 普通中文：

```c
usr_printf("[INIT] 系统初始化完成\r\n");
usr_printf("[UART] 接收超时\r\n");
usr_printf("[BMS] 通信失败\r\n");
usr_printf("[CHG] 进入预充阶段\r\n");
usr_printf("[ERR] 输出过压\r\n");
```

禁止在源码日志中新增 emoji：

```c
usr_printf("🎬 进入流程\r\n");
usr_printf("✅ 成功\r\n");
usr_printf("❌ 失败\r\n");
usr_printf("⚠️ 告警\r\n");
```

---

# 十一、编码与老工程保护偏好

用户经常维护老工程，可能存在 GBK / CP936 / ANSI / UTF-8 混合编码。

AI 修改源码时必须特别注意：

1. 不要擅自转码。
2. 不要整文件重写。
3. 不要批量格式化。
4. 不要改变行尾。
5. 不要新增 emoji 或特殊 Unicode 图标。
6. 不确定编码时，先停止，给人工补丁。
7. 编码保护规则以项目根目录 `AGENTS.md` 为准。

补充偏好：

```text
如果项目有 Git 管理，并且目标文件已被 Git 跟踪，允许写入时不需要额外生成 .bak 备份。
写入后必须提醒用户看 git diff。
如果不是 Git 仓库，或目标文件未被 Git 跟踪，则写入前仍建议备份。
```

---

# 十二、代码修改偏好

用户更喜欢“小改动、可验证、可回退”的修改方式。

修改代码时应遵守：

1. 优先最小修改。
2. 不做无关重构。
3. 不顺手改风格。
4. 不擅自重命名无关变量。
5. 不重排函数。
6. 不重排 include。
7. 不修改与任务无关的注释和日志。
8. 给出修改原因。
9. 给出可能影响。
10. 有风险时给回退方案。
11. Git 工程写入后用 `git diff` 审查。
12. 非 Git 工程写入前保留备份。

如果不能安全直接改文件，应输出人工合入补丁。

---

# 十三、补丁与行号偏好

当输出人工合入补丁时，用户希望有明确定位信息与注释加日期。

必须包含：

```text
文件路径
函数名 / 模块位置
原始行号范围
原代码片段
新代码片段
修改原因
编码风险说明
```

行号只能作为辅助定位，必须同时提供函数名和原代码片段。

---

# 十四、状态机与任务设计偏好

用户偏好清晰的状态机和周期调度。

推荐设计方式：

```text
初始化阶段：完成外设、变量、默认状态初始化
周期任务：例如 1ms / 10ms / 100ms 调度
状态机：只处理当前状态的动作和跳转
中断：只做短小处理
通信：接收中断入 FIFO，主循环解析
保护：过压、欠压、过温、通信超时等独立判断
```

状态机代码应避免：

1. 大量阻塞延时；
2. 多处重复跳转；
3. 状态含义不清；
4. 故障恢复条件不明确；
5. 计数器用途混乱；
6. 通信和充电流程强耦合。

---

# 十五、通信协议代码偏好

用户常处理 UART / 单线 UART / CAN / 自定义协议。

AI 写协议代码时应注意：

1. 接收建议使用中断 + FIFO。
2. 发送一般不需要复杂中断，除非项目要求。
3. 协议解析应有明确帧头、长度、命令、校验、超时处理。
4. 连续错误应计数，不要一次失败就永久异常。
5. 通信失败后的恢复策略要留接口给主流程。
6. 不要把复杂协议逻辑全部塞进中断。
7. 大小端必须明确。
8. 校验算法必须清晰注释。

---

# 十六、ADC / 滤波 / 控制算法偏好

用户常处理 ADC 采样、滤波、温度补偿、电压电流控制。

默认偏好：

1. 使用整数算法。
2. 使用滑动平均、中值、IIR、限幅等轻量算法。
3. 避免浮点。
4. 明确 ADC 原始值、工程单位和换算关系。
5. 标定值必须有异常保护。
6. 查表插值必须写清楚比例和边界。
7. 阈值判断建议加滞回或连续计数。

---

# 十七、工程文件输出偏好

用户经常要求 Markdown 文件、说明文档、流程图、代码规范。

输出文档时优先：

1. 使用 `.md`；
2. 结构清晰；
3. 标题分级明确；
4. 表格适度；
5. 可直接复制到项目；
6. 不要只给散乱说明。

如果用户要求“供下载”，应生成可下载文件。

---

# 十八、AI 协作边界

AI 不应假设自己知道全部项目背景。

当项目资料不足时：

1. 可以基于常见嵌入式、上位机、APP 工程经验给建议；
2. 必须标明假设条件；
3. 不要编造芯片寄存器；
4. 不要编造协议字段；
5. 不要编造硬件连接；
6. 不要编造 APP 页面或接口；
7. 不确定时给需要核对的清单。

---

# 十九、对 AI 的强制提醒

开始处理用户工程前，AI 应优先理解：

```text
用户要的不是“看起来高级”的代码，
而是能在老工程、小 MCU、上位机工具、设备 APP、量产环境中稳定运行、容易维护、容易人工合入的代码。
```

如果涉及源码修改，应先考虑：

```text
会不会改坏编码？
会不会覆盖用户未提交改动？
会不会引入浮点？
会不会过度重构？
会不会改变原风格？
会不会影响量产稳定性？
会不会让人工难以合入？
会不会让测试人员不好用？
```

---

# 二十、推荐协作方式

处理复杂任务时，建议按以下顺序：

1. 先确认目标；
2. 再分析现有代码；
3. 判断项目类型：嵌入式 / 上位机 / APP / 网页 / 文档；
4. 检查 Git 状态和编码风险；
5. 列出风险点；
6. 给出最小修改方案；
7. 给出人工合入补丁或完整函数；
8. 说明测试方法；
9. 说明回退方式。

---

# 二十一、总结

用户偏好：

```text
中文回答；
工程实用；
保守稳定；
小步修改；
少用浮点；
不用 malloc；
重视编码；
重视 Git diff；
重视注释；
重视状态机；
重视串口工具体验；
重视设备 APP 稳定；
重视可量产；
重视人工可合入；
重视老板/客户能看懂。
```

AI 应始终按以下思路协作：

```text
嵌入式老工程维护
+ PC 串口工具开发
+ 手机端设备 APP
+ 量产稳定性
+ 编码安全
+ Git 可回退
+ 文档可交付
```

---

# 二十二、CodeGraph 协作偏好

用户希望在使用 Codex / Cursor / Claude Code 等 AI 工具处理代码时，尽量配合 CodeGraph 做代码关系分析。

CodeGraph 在用户工作流中的定位：

```text
CodeGraph 用来查代码地图。
AI 工具用来分析、修改、审查代码。
用户最终通过 git diff、编译、实测来确认结果。
```

## 1. 默认使用场景

遇到以下任务时，AI 应优先考虑使用 CodeGraph：

1. 查函数调用链；
2. 查谁调用了某个函数；
3. 查某个函数又调用了哪些函数；
4. 查全局变量在哪里读写；
5. 查状态机影响范围；
6. 查串口 / CAN / 蓝牙协议收发流程；
7. 查 ADC / PWM / GPIO / 继电器控制路径；
8. 查 APP 页面、store、protocol、types 之间的关系；
9. 查上位机串口工具的数据流；
10. 修改代码前做影响分析。

## 2. 用户偏好的 CodeGraph 输出方式

用户不喜欢只给抽象结论。

使用 CodeGraph 后，AI 应尽量输出：

```text
目标函数：
所在文件：
上游调用：
下游调用：
相关变量：
相关文件：
影响范围：
风险点：
建议最小修改位置：
```

对嵌入式工程，还要补充：

```text
是否涉及中断：
是否涉及定时器：
是否涉及状态机：
是否涉及通信超时：
是否涉及硬件控制：
是否可能影响量产稳定性：
```

## 3. 修改前的固定要求

AI 在修改代码前，应尽量先说清楚：

```text
我先用 CodeGraph 查调用链和影响范围。
确认只改最小范围后，再给补丁或写入方案。
```

如果 CodeGraph 结果不完整，应直接说明：

```text
CodeGraph 结果不完整，需要结合源码继续核对，不能只凭图谱下结论。
```

## 4. 与用户代码风格的关系

即使使用 CodeGraph，也必须继续遵守用户原有偏好：

1. 中文回答；
2. 小改动；
3. 不无关重构；
4. 不擅自格式化；
5. 不擅自转码；
6. 不新增 emoji；
7. 嵌入式代码优先兼容 C89/C90；
8. 不默认使用 malloc / float / 递归；
9. 写入前遵守 ENCODING_GUARD；
10. Git 工程写入后提醒查看 git diff。

## 5. 用户认可的工作流

推荐工作流：

```text
1. 读取 AGENTS.md 和 USER.md；
2. 检查是否存在 .codegraph；
3. 使用 CodeGraph 分析调用关系；
4. 输出影响范围和风险点；
5. 给出最小修改方案；
6. 用户确认允许写入；
7. 按编码保护和 Git 保护规则修改；
8. 输出修改报告；
9. 用户通过 git diff、编译和实机测试确认。
```

## 6. 一句话偏好

```text
用户希望 AI 不要边找边猜，而是先用 CodeGraph 查清楚代码关系，再按最小修改原则处理。
```

