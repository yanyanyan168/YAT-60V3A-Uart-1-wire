# 一线通信异步轮询代码实现思路

## 1. 改造目标

本次重构的目标不是单纯减少代码行数，而是让协议收发和充电流程更容易跟踪、调试和交接。

核心原则：

```text
1. 通信收发异步，不再“发一帧等一帧回复”。
2. uart_1_wire.c 只负责一线通信协议。
3. ch.c 只负责充电流程控制。
4. 通信层不要直接决定充电状态。
5. 充电流程不要直接解析协议字节。
6. 小函数不过度泛滥，变量不过度分散。
7. 宏定义只保留真正需要统一配置的内容。
```

最终希望同事打开代码后，能按下面思路理解：

```text
接收：一直扫合法帧，扫到就更新数据。
发送：按阶段每 100ms 发一帧，不等回复。
充电：看握手是否完成、通信是否超时、目标电压电流是多少。
异常：按充电状态机处理，不让协议层乱跳状态。
```

---

## 2. 文件职责划分

### 2.1 uart_1_wire.c

负责一线通信协议本身，包括：

```text
1. 接收 FIFO 数据。
2. 扫描合法帧。
3. 校验和判断。
4. 解析 A0/A1/A4/A6/A7/B1/B3/B4 等数据。
5. 按阶段周期发送协议帧。
6. 维护通信超时计时。
7. 维护关键帧刷新计时。
8. 主动拉低 COM。
9. 给 ch.c 提供统一的通信结果。
```

`uart_1_wire.c` 不负责：

```text
1. 不直接切换充电状态。
2. 不直接判断充电器过压/过流/硬件异常。
3. 不决定是否进入 CH_FULL / BMS_ERR / CH_IDLE。
```

---

### 2.2 ch.c

负责充电流程本身，包括：

```text
1. 电池插入检测。
2. 启动握手。
3. 握手完成后进入充电判断。
4. 控制继电器、PWM、输出。
5. 根据协议结果设置目标电压和目标电流。
6. 判断充电器自身异常。
7. 判断通信超时后进入异常。
8. 判断电池拔出后回待机。
9. 满电、异常、温度异常等状态处理。
```

`ch.c` 不负责：

```text
1. 不解析协议帧。
2. 不关心当前发到 A0 还是 B4。
3. 不处理校验和。
4. 不处理接收缓存。
```

---

## 3. 通信总体模型

本次采用异步轮询模型。

旧思路：

```text
发 A0
等 A0 回复
超时重试
收到后再发 A1
```

新思路：

```text
发送任务：
每 100ms 按当前阶段发送下一帧。

接收任务：
随时从缓存里找合法帧，找到就解析。

超时任务：
只要合法帧长时间没有更新，就认为通信异常。
```

这样可以去掉大量同步等待变量，例如：

```text
wait_cmd
wait_len
wait_start_ms
retry_count
task_state
busy
can_send
```

通信层只保留少量必要状态：

```text
stage              当前通信阶段
tx_index           当前发送表下标
tx_tick_10ms       发送节拍
rx_len             接收缓存长度
any_rx_age_10ms    任意合法帧未刷新时间
key_age_10ms[]     关键帧未刷新时间
full_display_10ms  满电显示计时
```

---

## 4. 通信阶段设计

通信阶段建议如下：

```c
U1W_STAGE_STOP          // 停止通信，释放 COM
U1W_STAGE_HANDSHAKE     // 握手阶段
U1W_STAGE_CHARGE        // 正常充电阶段
U1W_STAGE_FULL_DISPLAY  // 满电显示阶段
U1W_STAGE_TEMP_WAIT     // 温度异常等待恢复
U1W_STAGE_PULL_LOW      // 主动拉低 COM
```

各阶段含义：

| 阶段 | 行为 |
|---|---|
| STOP | 不发协议帧，释放 COM |
| HANDSHAKE | 每 100ms 轮询 A0/A1/A4/A6/A7/B1/B3/B4 |
| CHARGE | 每 100ms 轮询 B1/B3/B4/B6 |
| FULL_DISPLAY | 每 100ms 发送 B6 显示 SOC，持续 3 分钟 |
| TEMP_WAIT | 温度异常时继续轮询 B3/B4 |
| PULL_LOW | 主动拉低 COM，通知 BMS 主机结束通信 |

---

## 5. 发送任务实现思路

发送任务函数：

```c
static void u1w_tx_task(void);
```

设计原则：

```text
1. 每 100ms 发送一帧。
2. 不等待对应回复。
3. 不做单帧重试。
4. 当前阶段决定发送表。
5. 发送失败只记录错误，不在通信层切充电状态。
```

握手阶段发送表：

```c
A0 / A1 / A4 / A6 / A7 / B1 / B3 / B4
```

A1 强制参与握手，避免基础数据不完整。

充电阶段发送表：

```c
B1 / B3 / B4 / B6
```

B6 在充电阶段固定要求打开充电 MOS：

```text
B6 01 01 CHK
```

BMS 普通异常由 BMS 自己处理 MOS，充电器不再根据 B4 异常主动关闭 BMS MOS。

满电显示阶段：

```text
发送 B6 03 SOC
持续 3 分钟
3 分钟后进入 PULL_LOW
```

温度异常等待阶段：

```text
继续发送 B3 / B4
用于检查温度是否恢复
不主动拉低 COM
```

---

## 6. 接收任务实现思路

接收任务函数：

```c
static void u1w_rx_task(void);
```

接收不再假设缓存第 0 字节就是帧头，而是扫描式找 CMD。

处理流程：

```text
1. 先判断 FIFO 是否接收空闲。
2. FIFO 长度连续两次相同，认为本批数据收完。
3. 将 FIFO 数据搬到协议接收缓存。
4. 从接收缓存第 0 字节开始 for/while 扫描。
5. 当前字节是已知 CMD，则根据 CMD 查固定回复长度。
6. 剩余长度不足，保留当前 CMD 及后续数据，等待下次接收。
7. 长度足够，计算校验和。
8. 校验成功，解析本帧，并删除本帧及前面的无效数据。
9. 校验失败，从下一字节继续找可能帧头。
```

这样做的好处：

```text
1. 前面有垃圾字节也能恢复。
2. 丢字节后不会整包死掉。
3. 校验失败不会清空全部缓存。
4. 下一帧如果完整，仍然能被找到。
```

合法帧判断条件：

```text
CMD 已知
长度足够
校验和正确
```

只有合法帧才允许：

```text
1. 更新协议数据。
2. 清任意合法帧计时。
3. 清对应关键帧计时。
4. 更新握手 mask。
5. 设置 link_state 在线。
```

---

## 7. 接收空闲判断

由于主循环每 10ms 调一次，如果 FIFO 里刚好正在进数据，立即搬运和解析可能会读到半帧。

因此增加模拟接收空闲判断：

```text
FIFO 有数据，但本次长度和上次不同：
    说明可能还在接收，先不解析。

FIFO 有数据，并且本次长度和上次相同：
    认为这一批数据已经收完，可以搬运并解析。
```

需要在通信控制结构里保存：

```c
u8 rx_fifo_last_cnt;
```

这样比加复杂串口空闲中断更简单，也适合当前 10ms 任务模型。

---

## 8. 超时设计

超时分两层。

### 8.1 任意合法帧超时

```text
2 秒没有收到任何合法帧，认为通信丢失。
```

对应计数：

```c
any_rx_age_10ms
```

收到任意合法帧后清零。

---

### 8.2 关键帧超时

仅收到某些非关键帧不能代表所有充电数据都有效。

因此关键帧各自计时：

```c
key_age_10ms[]
```

握手阶段关键帧：

```text
A0 / A1 / A4 / A6 / A7 / B1 / B3 / B4
```

充电阶段关键帧：

```text
B1 / B3 / B4
```

温度等待阶段关键帧：

```text
B3 / B4
```

如果当前阶段需要的关键帧超过 3 秒没有刷新，也判定通信异常，并记录超时的 CMD，方便调试。

---

## 9. 握手完成判断

握手阶段必须收齐：

```text
A0 / A1 / A4 / A6 / A7 / B1 / B3 / B4
```

每收到一个合法关键帧，就置位对应 mask。

全部收齐后：

```text
handshake_ok = 1
```

`ch.c` 看到握手完成后，才允许进入充电判断流程。

这样可以避免只收到 B4 或 B1 就误启动充电。

---

## 10. 满电处理

满电状态不能直接停止通信。

正确流程：

```text
1. 关闭充电输出。
2. 关闭电池电压检测分压，避免长期放电。
3. 通信阶段切到 FULL_DISPLAY。
4. 周期发送 B6 03 SOC，持续 3 分钟。
5. 3 分钟后主动拉低 COM。
6. 后续间歇打开电压检测。
7. 如果电压回落，需要重新握手，而不是直接用旧数据继续充。
8. 如果确认电池拔出，清数据回 CH_IDLE。
```

满电后电压回落重新充电时，必须重新执行握手，避免复用上一轮旧数据。

---

## 11. 普通异常处理

普通异常包括：

```text
通信异常
充电器过压
充电器过流
硬件异常
预充超时
CCCV 超时
欠压异常
其他非温度类锁止异常
```

普通异常流程：

```text
1. 关闭充电输出。
2. 关闭电池电压检测分压。
3. 通信阶段切到 PULL_LOW。
4. 主动拉低 COM，通知 BMS 主机结束通信。
5. 间歇打开电压检测判断电池是否拔出。
6. 拔出确认后清数据，释放 COM，回 CH_IDLE。
```

注意：

```text
主机侧不需要检测“COM 被拉低 30ms”作为异常。
30ms 低电平是主机主动拉低给 BMS 判断主机断开的动作。
```

---

## 12. 温度异常处理

温度异常是例外，不能主动拉低 COM。

原因：

```text
温度异常时需要继续通信，等待温度恢复。
```

流程：

```text
1. 关闭充电输出。
2. 通信阶段切到 TEMP_WAIT。
3. 继续轮询 B3/B4。
4. 根据 B3/B4 判断温度是否恢复。
5. 温度恢复后重新握手。
6. 如果电池拔出，清数据回 CH_IDLE。
```

温度异常不走 PULL_LOW。

---

## 13. 电池电压检测分压控制

工程里电池电压检测由 IO 控制，例如：

```c
BATT_DIVIDER_EN
```

该分压检测会对电池造成放电，因此不能一直打开。

### 13.1 正常充电相关状态

可以常开：

```text
CH_IDLE
BMS_HANDSHAKE
CH_Check
CH_REPAIR
CH_Pre1
CH_CCCV
```

这些状态需要持续看电压。

### 13.2 满电与异常锁止状态

必须默认关闭：

```text
CH_FULL
BMS_ERR
CH_TimOut
CCCV_TimOut
CH_UVP
CH_OVP
CH_OCP
NTC_ERR
HW_ERR
```

只允许间歇打开检测。

建议窗口：

```text
每 1 秒打开一次
打开保持 100ms
打开 50ms 后认为 ADC 有效
```

只有窗口有效时，才允许使用 `val.vout` 判断电池是否拔出或满电后是否回落。

---

## 14. 电池拔出判断

除 CH_IDLE 外，所有状态都需要判断电池拔出。

拔出优先级最高：

```text
拔电池 > 通信超时 > 温度恢复 > 满电回落 > 正常充电判断
```

拔出确认后统一处理：

```text
1. 关闭输出。
2. 停止通信。
3. 释放 COM。
4. 清上一块电池协议数据。
5. 清充电临时状态。
6. 回 CH_IDLE。
```

---

### 14.1 继电器未闭合或输出关闭时

可以用电压判断：

```text
val.vout < vRESET
连续确认一段时间
```

满电和异常状态需要结合间歇电压检测窗口。

---

### 14.2 充电中继电器已闭合时

不能用电压判断拔出。

原因：

```text
继电器闭合后，val.vout 可能是充电器自己的输出电压。
电池拔掉后，只要输出还在，电压仍可能存在。
```

充电中应使用：

```text
输出电流小 + 1 秒无合法通信帧
```

建议条件：

```text
val.curr < iEND / 2
并且
no_rx_10ms >= 100
```

也就是：

```text
输出电流很小
且
1 秒没有收到任何合法 BMS 帧
```

这样优先判断为电池拔出，而不是通信异常。

如果 2 秒没有通信，但电流不小，则按通信异常处理。

---

## 15. ch.c 主流程建议

`usr_ch_func()` 每 10ms 调用一次，建议顺序：

```text
1. 运行 uart_1_wire_poll_10ms()
2. 获取通信信息
3. 根据当前状态控制 BATT_DIVIDER_EN
4. 非 CH_IDLE 状态先判断电池拔出
5. switch(ch_state) 处理状态
```

状态处理大致如下：

```text
CH_IDLE：
    检测电池插入
    插入后 reset_link
    切到 HANDSHAKE

BMS_HANDSHAKE：
    等待握手收齐
    收齐后切到 CH_Check

CH_Check：
    根据当前电压决定进入修复、预充、CCCV 或满电

CH_REPAIR / CH_Pre1 / CH_CCCV：
    保持通信阶段 CHARGE
    优先判断拔出
    再判断通信超时
    再判断充电器自身异常
    最后执行正常充电控制

CH_FULL：
    关闭输出
    FULL_DISPLAY 三分钟后 PULL_LOW
    间歇检测电压
    电压回落后重新握手

BMS_TEMP_ERR：
    关闭输出
    TEMP_WAIT 继续通信
    温度恢复后重新握手

其他异常：
    关闭输出
    PULL_LOW
    间歇检测拔电池
```

---

## 16. 日志打印规则

默认不要打开协议收发日志，否则异步 100ms 轮询会刷屏。

建议默认：

```c
#define U1W_DEBUG_EN        0
#define U1W_DEBUG_PARSE_EN  0
```

默认只保留状态切换类日志。

需要现场抓协议时再临时打开。

日志建议区分：

```text
状态切换日志
通信发送日志
通信接收成功日志
通信校验失败日志
关键帧超时日志
通信总超时日志
拔电池日志
满电显示结束日志
COM 主动拉低日志
```

---

## 17. 为什么这个实现比同步等待更简单

同步等待写法的问题：

```text
1. 每发一帧都要记正在等谁。
2. 需要 wait_cmd / wait_len / wait_start_ms。
3. 收到非当前等待帧时不好处理。
4. retry 和 busy 状态容易和充电状态机冲突。
5. 调试时很难知道为什么不能发下一帧。
```

异步轮询写法的优点：

```text
1. 发送只按表轮询。
2. 接收只认合法帧。
3. 数据是否新鲜由计时器判断。
4. ch.c 只看结果，不关心协议细节。
5. 状态机更像实际充电流程。
6. 代码更容易给同事解释。
```

---

## 18. 后续检查顺序

打开电脑后建议按这个顺序检查：

```text
1. Keil Rebuild，先解决语法/类型问题。
2. 检查 P30/COM 方向控制是否和硬件一致。
3. 检查 BATT_DIVIDER_EN 高低电平是否和硬件一致。
4. 检查握手是否能收齐 A0/A1/A4/A6/A7/B1/B3/B4。
5. 检查充电阶段是否按 B1/B3/B4/B6 轮询。
6. 检查 B6 01 01 是否正确发送。
7. 检查满电是否发送 B6 03 SOC 3 分钟。
8. 检查满电后是否主动拉低 COM。
9. 检查普通异常是否主动拉低 COM。
10. 检查温度异常是否继续通信，不拉低 COM。
11. 检查异常/满电时 BATT_DIVIDER_EN 是否默认关闭。
12. 检查间歇打开电压检测时 ADC 是否稳定。
13. 检查继电器闭合充电中拔电池是否用“小电流 + 1 秒无通信”判断。
14. 检查拔电池后是否清协议数据并回 CH_IDLE。
15. 检查默认日志是否不刷屏。
```

---

## 19. 总结

本次实现思路可以概括为：

```text
uart_1_wire.c：
    用最少状态做异步通信。
    接收扫帧，发送轮询，超时计时，主动拉低 COM。

ch.c：
    用通信层给出的结果控制充电流程。
    负责充电、满电、异常、拔电池、分压检测开关。

整体：
    不同步等待，不复杂 retry，不让协议层乱跳充电状态。
```

最终目标是：

```text
协议层像通信协议。
充电层像充电流程。
两边边界清楚，代码顺着读。
```
